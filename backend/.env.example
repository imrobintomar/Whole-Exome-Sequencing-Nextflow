# Backend Environment Variables

# CORS Origins (comma-separated list)
# Add your Vercel deployment URL here after deploying
CORS_ORIGINS=http://localhost:3000,https://your-app.vercel.app,https://your-app-*.vercel.app

# Secret key for JWT tokens (change this!)
SECRET_KEY=your-secret-key-change-this-to-something-secure

# Database
DATABASE_URL=sqlite:///./wes_pipeline.db

# Directories (already configured in config.py)
# UPLOAD_DIR=/path/to/uploads
# RESULTS_DIR=/path/to/results

# Nextflow configuration (already in config.py)
# NEXTFLOW_SCRIPT=/path/to/main.nf
# REFERENCE_GENOME=/path/to/hg38.fa
